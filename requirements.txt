# Laundromat Client Dependencies

# Core dependencies
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=8.0.0

# HTTP client for server communication
requests>=2.28.0

# Note: The client does NOT need PyTorch or SAM3 - 
# those run on the server side only.
# Install server dependencies separately in server/requirements.txt
